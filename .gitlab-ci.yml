image: docker

services:
  - docker:dind

stages:
  - test
  - deploy

test:
  stage: test
  script:
  - echo run tests in this section

step-deploy-docker-deployment:
  stage: deploy
  only:
  - docker-deployment
  script:
  - docker-compose up -d --build
  environment: production
  when: manual
