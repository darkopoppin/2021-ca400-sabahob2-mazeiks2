image: docker

services:
  - docker:dind

stages:
  - test
  - deploy

test:
  stage: test
  script:
  - echo run tests in this section

step-deploy-master:
  stage: deploy
  only:
  - master
  script:
  - docker-compose build
  - docker-compose up -d
  environment: production
  when: always
